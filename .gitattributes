# 默认行为：文本文件保持原样
* text=auto

# 显式声明文本文件，保持其原有的行尾
*.py text
*.rst text
*.md text
*.txt text
*.yml text
*.yaml text
*.json text
*.toml text
*.ini text
*.cfg text

# 明确使用LF的文件
*.sh text eol=lf
Makefile text eol=lf
*.bash text eol=lf

# 明确使用CRLF的文件（Windows特定）
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 声明二进制文件（不进行换行符转换）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.tar binary
*.gz binary
